<!-- res/color/text_color_selector.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Inactive state -->
    <item android:color="@color/inactiveTextColor" android:state_enabled="false"/>

    <!-- Default state -->
    <item android:color="@color/defaultTextColor"/>
</selector>
